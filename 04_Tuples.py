# Tuples are similar to lists but they are immutable- they have less methods
# mydict.items() return tuples

mylist = [1, 2, 3]
mytuple = (1, 2, 3)

mylist[0] = "NEW"
print(mylist)

#  we can't add a new item to tuples
